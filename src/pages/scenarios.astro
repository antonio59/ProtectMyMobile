---
// The scenario slides now live in the news feed, with a page each under /slides.
// Kept as a permanent redirect so existing links and shares still land somewhere.
return Astro.redirect('/news?category=safety_slides', 301);
---
