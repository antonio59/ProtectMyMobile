name: Fallow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  analyze:
    name: Code Quality & Dead Code Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.5'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run Fallow (dead-code)
        run: npx fallow --only dead-code --fail-on-issues --format compact

      - name: Run Fallow (duplication)
        run: npx fallow --only dupes --format compact
        continue-on-error: true

      - name: Run Fallow (health)
        run: npx fallow --only health --format compact
        continue-on-error: true

      - name: Run Fallow (full + SARIF)
        run: npx fallow --format sarif --sarif-file=fallow-results.sarif
        continue-on-error: true

      - name: Upload SARIF to GitHub Security tab
        if: hashFiles('fallow-results.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: fallow-results.sarif
          category: fallow

      - name: Save regression baseline (main branch only)
        if: github.ref == 'refs/heads/main'
        run: |
          mkdir -p .fallow
          npx fallow --save-regression-baseline .fallow/regression-baseline.json --format compact

      - name: Check for regressions (PR only)
        if: github.event_name == 'pull_request'
        run: |
          if [ -f .fallow/regression-baseline.json ]; then
            npx fallow --fail-on-regression --regression-baseline .fallow/regression-baseline.json --format compact
          else
            echo "No baseline found; skipping regression check."
          fi
