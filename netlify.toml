[build]
  # Use npm for builds (handles native dependencies better than bun)
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
