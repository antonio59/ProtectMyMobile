[build]
  # Use npm for CI builds (handles cross-platform native deps better)
  # Use bun locally (faster)
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
