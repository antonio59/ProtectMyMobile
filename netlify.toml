[build]
  command = "curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install && ~/.bun/bin/bun run build"
  publish = "dist"

[build.environment]
  BUN_VERSION = "1.3.3"

[functions]
  directory = "netlify/functions"
